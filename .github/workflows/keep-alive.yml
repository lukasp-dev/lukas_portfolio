name: Keep Notion Server Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Notion Server
        run: curl -s -o /dev/null -w "%{http_code}" https://notion-server.jewook.com
